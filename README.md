# مجرة — ملفات APK

ملفات التثبيت الجاهزة لتطبيق **«مجرة»** (إدارة المواقع الشخصية — أوفلاين 100٪).

## الملفات

| الملف | الوصف |
|---|---|
| `galaxy-2.5.1-minified.apk` | ✅ **الأحدث** — حزمة الإصدار **2.5.1** نسخة مصغّرة (Release + R8 + shrinkResources) بحجم 3.6م فقط، موقّعة بمفتاح شخصي حتى تكون قابلة للتثبيت مباشرة (المتطلب: Android 11 / API 30 فأحدث). تشمل انيميشنات واجهة المجرة الثلاثين. |
| `galaxy-2.5.0-minified.apk` | حزمة الإصدار **2.5.0** نسخة مصغّرة (3.6م) — ميزة «واجهة المجرة» قبل الانيميشنات. |
| `galaxy-2.4.0-debug.apk` | حزمة الإصدار **2.4.0** موقعة بمفتاح debug — نسخة تطوير غير مصغّرة (57م). |

> ملاحظة: النسخة المصغّرة 2.5.1 موقّعة بمفتاح مختلف عن سابقاتها،
> لذا إن كانت نسخة أقدم مثبتةً على جهازك فاحذفها أولًا ثم ثبّت هذه
> (التحديث فوق توقيع مختلف غير ممكن في أندرويد).

## محتوى الإصدار 2.5.1 (النسخة المصغّرة)

تنفيذ المقترحات الثلاثين لانيميشن وتصميم «واجهة المجرة» (جلسة
`مقترحات_واجهة_المجرة.html`) — كل مقترح كتلة مرقّمة برقمه في
`GalaxyNetworkScreen.kt`:

- **العقد (1–8)**: تنفس ذاتي بطور متفرد، ذيل مذنّب أثناء السحب، هالة
  متنفسة بلون التصنيف، موجة نقر، ظهور متدرج من مركز المجرة بارتداد،
  تمايل المرشحين نحو مصدر الربط، نبضة قلب مزدوجة عند نجاح الربط،
  وانهيار العقدة عند حذف الموقع.
- **الخطوط (9–14)**: أقواس منحنية بتدرج لوني بين تصنيفي الطرفين
  وشرطات متدفقة وإشارة ضوئية تعبر الرابط ورسم تدريجي عند الدخول
  وسماكة مرنة أثناء السحب وامتداد الرابط الجديد من جهة مصدره.
- **السماء (15–20)**: نجوم حتمية تومض بهدوء وتنزاح مع التحريك (عمق)،
  سدم منجرفة، توهج مركزي متنفس، وحلقات رادار خافتة.
- **الحركة (21–26)**: دخول بدوران خفيف، ملاءمة عرض بنابض، أسماء
  تظهر مع مستوى التكبير، غبار ينجذب لمصدر الربط، نقطة مدارية حول
  أيقونة شعار الربط، وشهاب نجاح يعبر الشاشة.
- **التصميم (27–30)**: عقد بيضاء بحلقات خارجية ملونة، حجم العقدة
  حسب عدد روابطها، وخطوط أرق وأنعم بطابع خرائط الأبراج.

## محتوى الإصدار 2.5.0

- ميزة «واجهة المجرة»: تبويب جديد يعرض المواقع نقاطًا بأسمائها وخطوط
  الروابط بينها كشبكة اتصالات، مع تحريك وتكبير بإصبعين وسحب أي عقدة
  لإعادة ترتيبها وزر ملاءمة العرض.
- نقرة على موقع تفتح قائمة: «الانتقال للموقع» و«ربط موقع» مع فك
  الروابط القائمة، وجدول `site_links` (قاعدة البيانات إصدار 7).

## نتيجة التحقق من البناء

آخر فحص بناء عميق مستقل (19 سبتمبر 2026) — بناء كامل عبر `./gradlew`
بعد تثبيت JDK 17 وAndroid SDK (platform 34 + build-tools 34.0.0):

- `testDebugUnitTest` ✅ — 33/33 اختبارًا ناجحًا
  (9 لتوزيع المجرة + 7 لمحقق النزول الطارئ + 11 لسطور المواد
  + 6 لمحقق الموقع)
- `build` كامل ✅ — 106 مهمة بلا أي خطأ: تجميع، اختبارات،
  `lint` (مع `abortOnError`)، و`assembleDebug` و`assembleRelease`
- `assembleRelease` ✅ — تصغير R8 + shrinkResources + lintVital
  بلا أخطاء (59م في نسخة التطوير ← 3.6م في النسخة المصغّرة)
- `zipalign -c 4` ثم توقيع بـ `apksigner` والتحقق بـ
  `apksigner verify` ✅ (شهادة: `CN=Majarra Galaxy, O=Majarra, C=IQ`)
- فحص الانيميشنات الجديدة بصريًا غير ممكن في بيئة بلا محاكي،
  لكنها جُمّعت ومرّت بكل فحوصات Lint الصارمة للتكوين والموارد.

مواصفات البناء: Gradle 8.5 · AGP 8.2.2 · Kotlin 1.9.22 · compileSdk 34.

المصدر: مستودع `SagedAMV/app_work_kotlin` (الالتزام `06bb29e` — رأس الفرع
الرئيسي وقت بناء هذه الحزمة).
